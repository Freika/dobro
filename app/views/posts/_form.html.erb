<%= simple_form_for(@post) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :content, input_html: { rows: 10 } %>
  </div>
  <br>
  <div class="form-actions">
    <%= f.button :submit, 'Сохранить', class: 'btn btn-primary pull-right' %>
  </div>
<% end %>
