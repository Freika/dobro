# PostgreSQL
#   Ensure the PostgreSQL gem is defined in your Gemfile
#   gem 'pg'
development:
  adapter: postgresql
  encoding: utf8
  database: dobro_development
  host: localhost
  pool: 5
  port: 5432

test:
  adapter: postgresql
  encoding: utf8
  database: dobro_test
  host: localhost
  pool: 5
  port: 5432
